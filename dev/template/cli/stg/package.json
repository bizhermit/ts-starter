{
  "name": "stg",
  "version": "0.0.0",
  "description": "stg env",
  "private": true,
  "scripts": {
    "build": "npx tsc -p tsconfig.src.json",
    "cli": "npm run build && node bin/cli",
    "js": "npm run build && node js-index",
    "ts": "npm run build && npx tsc -p tsconfig.json && node ts-index"
  }
}
