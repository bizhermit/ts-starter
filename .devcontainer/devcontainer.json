{
  "name": "ts-starter",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "node",
  "workspaceFolder": "/workspace",
  // "remoteUser": "node",
  "postCreateCommand": "npm i && cd /workspace/src && npm i && cd /workspace/dev && npm i"
}