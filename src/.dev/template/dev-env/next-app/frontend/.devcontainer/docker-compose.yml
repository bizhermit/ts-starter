version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: __appName__
    tty: true
    volumes:
      - type: bind
        source: ../
        target: /workspace